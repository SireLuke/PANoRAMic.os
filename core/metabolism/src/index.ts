export function metabolism() {
    return {
        status: "ready",
        par: "pending-api",
        pool: "pending-api",
        projectFund: "pending-api",
        renewables: "pending-api"
    };
}
