export function renewables() {
    return {
        status: "ready",
        solar: "pending-api",
        wind: "pending-api",
        hydro: "pending-api",
        geothermal: "pending-api",
        wastevate: "pending-api"
    };
}
